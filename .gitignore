# next.js
/.next/
/out/
.next/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
node_modules

/lib/generated/prisma

# database dumps / local-only backups
*.sql
*.sqlite
*.sqlite3
/backups/

# local scratch scripts and debug logs (never commit real data/queries)
/scratch/

# local design-tool cache
/.kombai/

# uploaded user content (existing demo files stay tracked; new uploads are runtime data, not source)
/public/uploads/*
